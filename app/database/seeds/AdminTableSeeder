
<?php

class AdminTableSeeder extends \DatabaseSeeder {

  public function run( )
  {
    DB::statement('SET FOREIGN_KEY_CHECKS=0');
    # Disable FK constraints so that all rows can be deleted, even if there's an associated FK
    DB::statement('TRUNCATE admins');



      $admin = new Admin;
      $user->email      =  'mensah9@verizon.net.';
     $user->username = 'mensah9'
      $user->name = 'Norcius Noel';
      $user->password   = Hash::make('bety1126');
      $user->save();

  }

}
